#include "main.h"

/**
 * leet - check the code for
 *
 * @str : pointer.
 *
 * Return: Always 0.
 */

char *leet(char *str)
{
	int i = 0;

	if (str[i] == 1)
		return(str);
	else if (str[i] == 2)
		return(str);
	return (str);
}
